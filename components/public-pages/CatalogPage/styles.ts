import { makeStyles } from "@material-ui/core";
import stylesCardSectionProducts from "../../../styles/CardSectionProducts";

export default makeStyles((theme) => ({ 
  ...stylesCardSectionProducts(theme),
}))